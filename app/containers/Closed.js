import React, { Component } from 'react';

class Closed extends Component {
    constructor(props) {
        super(props);

        this.state = {
        };
    }

    render() {
        return (
            <div>
                Sorry, closed. Bitch!
            </div>
        );
    }
}

export default Closed;